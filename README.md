Running

make sure npm is installed, then just `npm run dev`

it should indicate what address it's running locally on your PC, e.g. `http://localhost:5176`, so navigating there will launch the website.
